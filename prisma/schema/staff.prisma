model Staff {
    id        String   @id @default(uuid())
    name      String
    title     String?
    photoUrl  String?
    email     String?
    phone     String?
    order     Int      @default(0)
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
