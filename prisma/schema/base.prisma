generator client {
    provider        = "prisma-client-js"
    output          = "../../lib/generated/prisma"
    previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
    provider = "postgresql"
}
